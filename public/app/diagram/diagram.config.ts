/// <reference path="../../../typings/tsd.d.ts" />

((): void => {

    'use strict';

    angular
        .module('diagram')
        .config(config);

    config.$inject = [];

    function config(): void {
    }

})();
